// تنظیمات ایستای اتصال به ایتایار (eitaayar.ir).
//
// ⚠️ توجه مهم (حتما بخوانید): طبق مستندات رسمی eitaayar.ir، این API فقط
// سه متد دارد: getMe، sendMessage، sendFile — هر سه فقط برای «ارسال» پیام
// از طرف ربات هستند. هیچ متد مستندی برای «دریافت» پیام‌های کاربران (نه
// webhook، نه polling) در این API وجود ندارد. برای همین، بر خلاف تلگرام و
// بله، اینجا نمی‌شود منوی شیشه‌ای تعاملی یا دیده‌بان شخصی ساخت. آنچه اینجا
// پیاده شده «ارسال گزارش نرخ به کانال ایتای شما» است.

// شناسهٔ پیش‌فرض کانال/چتی که گزارش نرخ‌ها در آن پست می‌شود، اگر در env
// مقداردهی نشده باشد.
export const DEFAULT_CHAT_ID = "nerkhemroozchand";
